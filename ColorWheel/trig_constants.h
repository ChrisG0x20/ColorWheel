//  Copyright (c) 2016 Christopher Gassib. All rights reserved.
//

#ifndef TRIG_CONSTANTS_H
#define TRIG_CONSTANTS_H

namespace trig
{

    const double Pi = 3.141592653589793;
    const double TwoPi = 2.0 * Pi;
    const double HalfPi = Pi / 2.0;
    const double OneAndHalfPi = Pi + HalfPi;
    const double ThirdPi = Pi / 3.0;
    const double QuarterPi = Pi / 4.0;

    const double _1Degree = Pi / 180.0;
    const double _2Degrees = Pi / 90.0;
    const double _3Degrees = Pi / 60.0;
    const double _4Degrees = Pi / 45.0;
    const double _5Degrees = Pi / 36.0;
    const double _6Degrees = Pi / 30.0;
    const double _7Degrees = (7.0 * Pi) / 180.0;
    const double _8Degrees = Pi / 22.5;
    const double _9Degrees = Pi / 20.0;
    const double _10Degrees = Pi / 18.0;
    const double _11Degrees = (11.0 * Pi) / 180.0;
    const double _12Degrees = Pi / 15.0;
    const double _13Degrees = (13.0 * Pi) / 180.0;
    const double _14Degrees = (7.0 * Pi) / 90.0;
    const double _15Degrees = Pi / 12.0;
    const double _16Degrees = Pi / 11.25;
    const double _17Degrees = (17.0 * Pi) / 180.0;
    const double _18Degrees = Pi / 10.0;
    const double _19Degrees = (19.0 * Pi) / 180.0;
    const double _20Degrees = Pi / 9.0;
    const double _21Degrees = (7.0 * Pi) / 60.0;
    const double _22Degrees = (11.0 * Pi) / 90.0;
    const double _23Degrees = (23.0 * Pi) / 180.0;
    const double _24Degrees = (2.0 * Pi) / 15.0;
    const double _25Degrees = Pi / 7.2;
    const double _26Degrees = (13.0 * Pi) / 90.0;
    const double _27Degrees = (3.0 * Pi) / 20.0;
    const double _28Degrees = (14.0 * Pi) / 90.0;
    const double _29Degrees = (29.0 * Pi) / 180.0;
    const double _30Degrees = Pi / 6.0;
    const double _31Degrees = (31.0 * Pi) / 180.0;
    const double _32Degrees = Pi / 5.625;
    const double _33Degrees = (11.0 * Pi) / 60.0;
    const double _34Degrees = (17.0 * Pi) / 90.0;
    const double _35Degrees = (7.0 * Pi) / 36.0;
    const double _36Degrees = Pi / 5.0;
    const double _37Degrees = (37.0 * Pi) / 180.0;
    const double _38Degrees = (19.0 * Pi) / 90.0;
    const double _39Degrees = (13.0 * Pi) / 60.0;
    const double _40Degrees = Pi / 4.5;
    const double _41Degrees = (41.0 * Pi) / 180.0;
    const double _42Degrees = (7.0 * Pi) / 30.0;
    const double _43Degrees = (43.0 * Pi) / 180.0;
    const double _44Degrees = (11.0 * Pi) / 45.0;
    const double _45Degrees = Pi / 4.0;
    const double _60Degrees = Pi / 3.0;
    const double _90Degrees = Pi / 2.0;
    const double _120Degrees = (2.0 * Pi) / 3.0;
    const double _180Degrees = Pi;
    const double _240Degrees = (4.0 * Pi) / 3.0;
    const double _300Degrees = (5.0 * Pi) / 3.0;
    const double _360Degrees = 2.0 * Pi;

} // namespace trig

#endif
